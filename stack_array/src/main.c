// Stack - Array based implementation
#include <stdio.h>
#include "stack.h"
#define MAX_SIZE 101

int
main(void)
{
  int A[MAX_SIZE];
  int n;
  int status = 1;

  while (status != 0)
    {
      status = scanf("%d", &n);
    }

  //printf("Enter an integer\n");
  //scanf("%d", &n);
  //push(A, n);
  
  //printf("Stack is %d\n", A[0]);

  return 0;
}
